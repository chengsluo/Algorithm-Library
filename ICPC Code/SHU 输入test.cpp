#include<iostream>
#include<string>
//#include<cstdio>
using namespace  std;

int main()
{
    string s;
    getline(cin,s,' ');//以/n为默认结束符
    cout<<s;
    //char str[10];
    //cin.getline(str,9,' ');//不以/n结束，size超过str的范围将发生未知的错误！！！//以/n为默认结束符
    //puts(str);
    //cout<<str;//和puts一样，自动换行
    cout<<"chengs"<<endl;
`
    return 0;
}
